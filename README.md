# neural-networks
#final note @MLPs:
#MLPs:

Treat each token in the context as independent (no memory or recurrence)

Lack awareness of position or order unless manually encoded

Cannot model temporal dependencies beyond fixed-size context